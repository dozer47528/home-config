rm -rf .oh-my-zsh
rm -rf .vim/bundle/Vundle.vim
rm -rf .zsh-plugins
rm -rf $ZSH_CUSTOM/plugins

mkdir $ZSH_CUSTOM/plugins

git submodule init
git submodule update

ln -s $HOME/.zsh-plugins/* $ZSH_CUSTOM/plugins

vim +PluginInstall +qall
